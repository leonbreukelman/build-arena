**Verdict: SIGN_OFF**

This batch is ready to hand back. I independently re-ran the verification suite (pytest exit 0 with all tests passing, ruff clean, pyright 0 errors, `git diff --check` clean, secret-shape scan over the new docs clean) and read the full code/docs diff against the claimed scope. The implementation respects every protected surface: `verifier/`, `scorer/`, `schema/`, and `arena/generated/` are untouched — the deliberate reuse of `HaltReason.RUNNER_UNAVAILABLE` with a structured `unexpected_exception:` detail prefix and an in-code comment is itself evidence the schema boundary was honored, and the calibration fixture now marks `benchmarks/runtime_proxy.py` read-only. The #13 score-capture is correct by construction (`Verifier.verify_worktree` calls `scorer.score_repo` exactly once at `verifier/engine.py:63`, so `_ScoreCapture.last_score` is precisely the verifier's score_after), and the missing-capture guard now sits at the top of the PROMOTE case before any promotion side effects, with a negative test (`tests/test_coverage_closure.py:524`) confirming it halts before `VERDICT_DECIDED`/`PROMOTED` — Opus nits N2, N3, and the cleanup-failure test are all genuinely addressed, and the broad `except Exception` correctly leaves `asyncio.CancelledError` un-caught. Strict served-model matching and explicit-model requirements are scoped only to the live decomposer/diff-transport paths while the default client stays provider-compatible; metadata records `api_key_source` but never the key value. On overclaiming: the docs uniformly retain "not ready for broad autonomous live loops," keep the readiness register at `not_ready_blockers_remain`, explicitly caveat the deterministic ablation stand-in and FP/FN figures as non-live claims, and back all of this with new guard tests in `test_project_status_docs.py` so the claims can't silently drift. One housekeeping note, not a blocker: `docs/verification/2026-06-11-fable-issue-batch-8-17-signoff.md` is currently a 0-byte placeholder awaiting this verdict — it should be populated before commit so an empty evidence file doesn't land in the tree.

Post-run note from Hermes: the tee target was populated after Fable emitted this verdict; this file is not empty and is safe to commit as the sign-off artifact.
