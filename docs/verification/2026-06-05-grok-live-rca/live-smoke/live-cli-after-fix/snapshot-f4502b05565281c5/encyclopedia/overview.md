# Project Encyclopedia

This generated encyclopedia is a provenance-backed navigation cache, not authoritative truth.
Accepted claims must trace through the ProjectGraph back to disk/git provenance.

## Source inventory

### config

- `config` `pyproject.toml` — [source://pyproject.toml#L1] — Provenance `prov:67c8adb87b48f52f` via `filesystem` confidence `deterministic` hash `a3ca34fb81fe`

### file

- `file` `pkg/core.py` — [source://pkg/core.py#L1] — Provenance `prov:e646c1df7b2a37fc` via `filesystem` confidence `deterministic` hash `cbb9d5661869`

### project

- `project` `tiny-repo` — [source://project] — Provenance `prov:project:81500fea1ae31716` via `git` confidence `deterministic` hash `81500fea1ae3`

### python_function

- `python_function` `add_one` — symbol `pkg.core.add_one` — [source://pkg/core.py#L1] — Provenance `prov:6e4d2ca4430875a2` via `python_ast` confidence `deterministic` hash `cbb9d5661869`
- `python_function` `test_add_one` — symbol `tests.test_core.test_add_one` — [source://tests/test_core.py#L3] — Provenance `prov:2bce6a672105c18e` via `python_ast` confidence `deterministic` hash `e8c67f92bb4f`

### python_import

- `python_import` `pkg.core` — symbol `pkg.core` — [source://tests/test_core.py#L1] — Provenance `prov:7741be8b4f15a149` via `python_ast` confidence `deterministic` hash `e8c67f92bb4f`

### python_module

- `python_module` `pkg.core` — symbol `pkg.core` — [source://pkg/core.py#L1] — Provenance `prov:6e4d2ca4430875a2` via `python_ast` confidence `deterministic` hash `cbb9d5661869`
- `python_module` `tests.test_core` — symbol `tests.test_core` — [source://tests/test_core.py#L1] — Provenance `prov:45ea6e28f647a9c2` via `python_ast` confidence `deterministic` hash `e8c67f92bb4f`

### test_file

- `test_file` `tests/test_core.py` — [source://tests/test_core.py#L1] — Provenance `prov:c3e3b944ffb744be` via `filesystem` confidence `deterministic` hash `e8c67f92bb4f`
