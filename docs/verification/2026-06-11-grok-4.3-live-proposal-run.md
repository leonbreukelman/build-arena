# Grok 4.3 Live Proposal Run — 2026-06-11

## Owner outcome

Status: passed after one small harness hardening found by the live run.

The actual Grok 4.3 proposal path was exercised against a temporary git repo through:

`OpenAICompatibleDiffTransport -> DiffProposerRunner -> validate_unified_diff -> git apply -> postcheck`

The final live attempt produced a valid unified diff, the deterministic patch gate accepted it, `git apply` changed the target file, and the postcheck confirmed `value() == 2`.

This remains a bounded smoke, not proof that broad autonomous live loops are ready.

## Budget

Operator budget: $10.00.

Safety margin used: 20% under the cap, so the mechanical safety cap was $8.00.

Pricing basis used for the estimate: Grok 4.3 at $1.25 / 1M input tokens and $2.50 / 1M output tokens.

Actual and upper-bound spend:

- First live proposal attempt: failed patch gate before provenance usage was persisted, so counted pessimistically at the pre-call worst-case estimate: `$0.00673875`.
- Second diagnostic live call: actual estimated cost `$0.00053750`; it saved raw visible output and confirmed the failure mode.
- Final fixed live proposal run: actual estimated cost `$0.00053375`.
- Combined conservative upper bound including prior attempts: `$0.01318375` using prior upper-bound + final actual, and `$0.01938875` using worst-case bounds.

Budget verdict: well under $10 and under the $8 safety cap.

## Model/provider confirmation

Model lookup before the run returned xAI model list containing `grok-4.3`.

Final run metadata:

- Provider: `xai`
- Requested model: `grok-4.3`
- Served model: `grok-4.3`
- Served model matched requested: `true`
- Finish reason: `stop`
- Prompt tokens: `299`
- Completion tokens: `64`
- Total tokens: `826`
- No markdown fence after transport normalization: `true`
- Final diff had trailing newline: `true`

## Live-run sequence

### Attempt 1: actual proposal runner smoke

Run root:

`/tmp/build-arena-grok43-live-proposal-20260611T130406Z`

Result:

- Status: `failed_runner_error`
- Error: `patch gate rejected: git_apply_check_failed`
- Pre-call worst-case cost estimate: `$0.00673875`

This proved the real xAI/Grok path was reachable and the deterministic patch gate correctly refused a malformed/applier-hostile output.

### Attempt 2: diagnostic raw-output capture

Run root:

`/tmp/build-arena-grok43-live-proposal-20260611T130406Z`

Artifacts:

- Raw visible output: `/tmp/build-arena-grok43-live-proposal-20260611T130406Z/attempt2-raw-visible-output.txt`
- Stripped diff: `/tmp/build-arena-grok43-live-proposal-20260611T130406Z/attempt2-stripped-diff.patch`
- Summary: `/tmp/build-arena-grok43-live-proposal-20260611T130406Z/attempt2-summary.json`

Result:

- Status: `gate_rejected`
- Gate reason: `git_apply_check_failed`
- Gate detail: `error: corrupt patch at line 8`
- First nonempty diff line: `diff --git a/src/app.py b/src/app.py`
- Served model: `grok-4.3`
- Finish reason: `stop`
- Raw output had no markdown fence.
- The visible diff was semantically right but missing the final newline; `git apply --check` treated that as a corrupt patch.

Finding: this was not a model-selection or provider-wiring failure. It was a small harness brittleness issue for a common LLM diff formatting quirk.

### Harness hardening applied

Changed files:

- `arena/runners/diff_proposer.py`
- `tests/test_diff_proposer.py`

Behavior added:

- `OpenAICompatibleDiffTransport` now appends one missing trailing newline after stripping an optional markdown fence and before patch-gate validation.
- Added regression test: `test_openai_compatible_diff_transport_adds_missing_final_newline`.

Targeted verification after the fix:

```bash
uv run pytest tests/test_diff_proposer.py::test_openai_compatible_diff_transport_adds_missing_final_newline tests/test_diff_proposer.py::test_diff_proposer_applies_live_transport_valid_diff_after_patch_gate -q
# PASS: 2 passed
```

### Final live proposal runner smoke

Run root:

`/tmp/build-arena-grok43-live-proposal-fixed-20260611T130807Z`

Artifacts:

- Patch: `/tmp/build-arena-grok43-live-proposal-fixed-20260611T130807Z/repo/.arena/patches/hyp-grok43-live-smoke-fixed.patch`
- Provenance: `/tmp/build-arena-grok43-live-proposal-fixed-20260611T130807Z/repo/.arena/patches/hyp-grok43-live-smoke-fixed.patch.provenance.json`
- Summary: `/tmp/build-arena-grok43-live-proposal-fixed-20260611T130807Z/summary.json`

Result:

- Status: `passed`
- Patch gate: accepted
- `git apply`: passed
- Postcheck: passed, `value() == 2`
- Final target file:

```python
def value() -> int:
    # Grok live proposal smoke should change this value only.
    return 2
```

Final run cost:

- Actual estimated cost: `$0.00053375`
- Prior-attempt upper bound + final actual: `$0.01318375`
- Worst-case bound including prior attempts: `$0.01938875`

## Repository verification after hardening

Commands run from `/home/leonb/projects/build-arena`:

```bash
uv run pytest tests/test_llm_adapter.py tests/test_diff_proposer.py -q
# PASS: 26 passed

uv run pytest tests/test_diff_proposer.py::test_openai_compatible_diff_transport_adds_missing_final_newline tests/test_diff_proposer.py::test_diff_proposer_applies_live_transport_valid_diff_after_patch_gate -q
# PASS: 2 passed

uv run pytest tests/test_diff_proposer.py tests/test_llm_adapter.py tests/test_project_model_cli_ai.py -q
# PASS: 33 passed

uv run pytest tests -q
# PASS: full suite passed

uv run ruff check .
# PASS: All checks passed

uv run pyright
# PASS: 0 errors, 0 warnings, 0 informations
```

## Conclusion

The actual Grok 4.3 proposal run is successful after the trailing-newline hardening discovered by live testing.

What is now proven:

- xAI API accepts `grok-4.3` for this OpenAI-compatible chat path.
- The proposal transport reaches the served `grok-4.3` model.
- Grok 4.3 can produce a unified diff for the proposal component.
- The transport normalizes the common missing-final-newline quirk without accepting prose or bypassing the deterministic patch gate.
- The deterministic patch gate, `git apply`, and postcheck all pass on the final bounded live smoke.
- Spend stayed far below the requested budget and the 20% safety margin.

Still not proven:

- A full autonomous Build Arena cycle on a real target repo.
- Broad unattended live-loop readiness.
- That Grok 4.3 will consistently produce valid diffs for larger or more ambiguous targets.
