# Build Arena -> fmc-mcp bounded production-run readiness report

Generated: 2026-06-14T22:03:19Z

## Verdict

Ready to perform ONE bounded local `fmc-mcp` production run after explicit operator authorization.

This is not broad unattended autonomy and not proof that live promotion already works in the wild. It is readiness for the first local, single-cycle, fail-closed promotion attempt against `/home/leonb/projects/fmc-mcp`.

## What was verified

### Build Arena

Final command:

```bash
make verify
```

Result: passed.

The command regenerated generated artifacts and confirmed no generated diff, then ran:

- `uv run ruff check .` — passed
- `uv run pyright` — 0 errors, 0 warnings, 0 informations
- `uv run pytest tests -q` — passed, full suite green

### fmc-mcp target repo

Final command:

```bash
uv run ruff check . && uv run python -m pytest -q && uv run python -m mypy src tests
```

Result: passed.

- Ruff: all checks passed
- Pytest: 19 passed
- Mypy: success, no issues in 11 source files

### Git state

Build Arena:

- Branch: `main...origin/main [ahead 1]`
- Has uncommitted remediation/report changes in this working tree.

fmc-mcp:

- Branch: `main...origin/main [ahead 1]`
- Latest local commit: `25f4458 chore: add Build Arena goal config`
- `.arena/goal.toml` exists in local HEAD and is required for the bounded run.

Local-only state is not a blocker for this one local run, but it remains a handoff/reproducibility blocker until pushed or PR'd.

## Blocker remediation implemented

1. Explicit live-call budget gate

- `arena/repo_goal_loop.py` now requires `live_max_calls` for live repo-goal modes.
- The loop computes planned live calls before any adapter invocation and rejects plans exceeding the cap.
- `RUN_STARTED` records `liveMaxCalls` and `plannedLiveCalls`.
- The cap now includes a conservative slot for `run_adversarial_probes` under live decomposition.
- Inert live flags now fail closed if neither `--decompose-mode live` nor `--apply-mode live_diff` is selected, preventing fake-live/offline runs copied from partial flag lists.

2. Stronger docs-candidate gate

- `arena/proposal_planner.py` now requires source references for documentation candidates by default.
- Proposal verification commands include `arena.markdown_links --require-source-references`.
- Deterministic docs generated by the loop include a `## Source references` section citing an existing repo file.
- Golden fixtures and tests were updated for this behavior.

3. Readiness register/docs reconciled

- `docs/verification/2026-06-05-pre-live-readiness-register.json` still reports `overallStatus=not_ready_blockers_remain` for broad autonomy, but now records a scoped `boundedFmcMcpProductionRun` exception.
- Active orientation docs (`README.md`, `AGENTS.md`, `docs/build-arena-project-brief.md`) now use the same scoped readiness language.
- Status docs now include the full live command flags, including `--decompose-mode live`, `--apply-mode live_diff`, `--live-api-key-env XAI_API_KEY`, and `--live-max-calls 2`.

## Opus verification

Plan review:

- Report: `/home/leonb/projects/build-arena/reports/2026-06-14-fmc-mcp-production-run-plan-opus-review.json`
- Verdict: `ACCEPT_WITH_CHANGES`
- Two blocking criticisms were accepted and implemented: explicit live-call cap; stronger docs-source-reference gate.

Implementation review:

- Report: `/home/leonb/projects/build-arena/reports/2026-06-14-fmc-mcp-production-run-implementation-opus-review.json`
- Verdict: `ACCEPT_WITH_CAVEATS`
- `blocksSingleBoundedFmcMcpRun`: false
- Blocking issues: 0

Opus gave two patch-before-run criticisms. Both were patched and re-verified:

1. The abbreviated operator flag list could create an inert fake-live run. Fix: register/tests/docs now require the full live-mode command, and `repo_goal_loop` rejects inert live flags.
2. `run_adversarial_probes` was not represented in the planned-call cap. Fix: `_planned_live_calls()` now counts a conservative probe slot for live decomposition with probes enabled.

## Exact command for the bounded production run

Run only after operator authorizes live spend and local target mutation:

```bash
uv run python -m arena.repo_goal_loop \
  --project /home/leonb/projects/fmc-mcp \
  --goal "Improve fmc-mcp repository health with one bounded verified change" \
  --artifacts-root /home/leonb/projects/build-arena/.arena/runs/fmc-mcp-production-$(date -u +%Y%m%dT%H%M%SZ) \
  --max-cycles 1 \
  --decompose-mode live \
  --apply-mode live_diff \
  --allow-live \
  --live-provider xai \
  --live-model grok-4.20-0309-non-reasoning \
  --live-api-key-env XAI_API_KEY \
  --live-max-tokens 12000 \
  --live-max-calls 2 \
  --test-command 'uv run python -m pytest -q' \
  --allow-promotion \
  --no-dry-run
```

Expected planned live calls for that command: 2.

## Remaining owner gates before running

These are not engineering blockers; they are owner/operator authorization gates:

1. Authorize live provider spend/API usage for this one run.
2. Authorize local mutation of `/home/leonb/projects/fmc-mcp` by ff-only promotion.
3. Ensure `XAI_API_KEY` is available in the runtime environment or configured secret source; do not print the key.
4. Accept that this is a local baseline mutation, not a PR workflow.

## Remaining caveats / non-blockers

- No live production promotion has been completed yet; this will be the first attempt.
- The previous verified live run was a dry-run and selected a documentation candidate, not a code candidate.
- Docs-source-reference gates prove links/citations exist, not semantic truth.
- There is no dashboard control plane or rollback endpoint; rollback handle is git history plus run artifacts.
- Broad unattended autonomy remains blocked by the readiness register.
- Build Arena and fmc-mcp local commits/changes still need push/PR for remote reproducibility.

## Bottom line

There is no verified engineering blocker left for one bounded local `fmc-mcp` production run using the full command above. The only remaining blockers are explicit operator authorization for live spend and local repo mutation, plus runtime credential availability.
