# GENERATED -- DO NOT EXECUTE. Dry-run command preview only.
# No OpenHands, OpenShell sandbox, GitHub call, push, merge, apply, or promote.
#!/usr/bin/env sh
set -eu
echo 'DRY RUN ONLY: no OpenHands execution, no OpenShell sandbox, no GitHub call.'
echo 'Hermes verification owner: hermes; execution: not_run; runtime: openshell_planned.'
echo 'Task objective: Run a read-only OpenShell CLI preflight and record evidence without creating a sandbox, starting a gateway, applying policy, calling GitHub, or mutating a target repository.'
# required verification: uv run python -m arena.maintainer.cli prepare --packet docs/examples/maintainer-openshell-preflight-packet.json --out .arena/maintainer-runs/openshell-preflight --force
# required verification: openshell --help
# required verification: openshell policy --help
# required verification: openshell status --help
# required verification: openshell doctor --help
# required verification: openshell doctor check
